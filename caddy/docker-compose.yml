version: '3.9'
services:
  caddy:
    image: caddy:2
    restart: always
    container_name: caddy
    network_mode: "host"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./volume/certs:/certs
      - ./volume/config:/config
      - ./volume/data:/data
      - ./volume/sites:/srv
