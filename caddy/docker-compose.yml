version: '3.9'
services:
  caddy:
    image: ghcr.io/siwonkh/caddy
    restart: always
    container_name: caddy
    network_mode: "host"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./certs:/certs
      - ./config:/config
      - ./data:/data
      - ./sites:/srv
