version: "3"
services:
  syncthing1:
    image: syncthing/syncthing
    container_name: syncthing1
    hostname: my-syncthing1
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - /syncthing/st-sync1:/var/syncthing
    network_mode: host
    restart: unless-stopped

  syncthing2:
    image: syncthing/syncthing
    container_name: syncthing2
    hostname: my-syncthing2
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - /syncthing/st-sync2:/var/syncthing
    network_mode: host
    restart: unless-stopped

  syncthing3:
    image: syncthing/syncthing
    container_name: syncthing3
    hostname: my-syncthing3
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - /syncthing/st-sync3:/var/syncthing
    network_mode: host
    restart: unless-stopped

  syncthing4:
    image: syncthing/syncthing
    container_name: syncthing4
    hostname: my-syncthing4
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - /syncthing/st-sync4:/var/syncthing
    network_mode: host
    restart: unless-stopped
